suite('History Tree View is correctly implemented', function() {
	test('GIVEN a historyEntry, getTreeItem SHOULD the item');
	test(
		'GIVEN Nothing, getChildren SHOULD return an array with all the tree items'
	);
	test(
		'GIVEN a Memento, refresh SHOULD get the list of history from the Memento and fire an event'
	);
});

suite('History Tree Item is correctly implemented', function() {
	test(
		'GIVEN correct inputs, HistoryEntry Constructor SHOULD return a correct object'
	);
});

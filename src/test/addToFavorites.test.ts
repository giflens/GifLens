suite('Add a Gif to Favorites works', function() {
	test('GIVEN the favorites are empty, SHOULD add it to the favorites');
	test(
		'GIVEN there is already a Gif in the Favorites, SHOULD add it to the list at the first position'
	);
	test(
		'GIVEN a Gif that was already in the favorites, SHOULD create an information message to inform the user'
	);
});

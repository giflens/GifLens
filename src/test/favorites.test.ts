suite('Favorites Tree View is correctly implemented', function() {
	test('GIVEN a FavoriteEntry, getTreeItem SHOULD the item');
	test(
		'GIVEN Nothing, getChildren SHOULD return an array with all the tree items'
	);
	test(
		'GIVEN a Memento, refresh SHOULD get the list of favorites from the Memento and fire an event'
	);
});

suite('Favorites Tree Item is correctly implemented', function() {
	test(
		'GIVEN correct inputs, FavoritesEntry Constructor SHOULD return a correct object'
	);
});
